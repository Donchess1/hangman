#!/usr/bin/python3
from category import *


class exam:
    def __init__(self):
        self.pool = pool
        self.word = word
        self.frag = frag


chosen_word = pick_category(pool)
swap_spaces(chosen_word)
