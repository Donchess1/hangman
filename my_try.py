#!/usr/bin/python3
import pick_category
import my_guess
import 

class exam:
    def __init__(self, pool, word, frag):
        self.pool = pool
        self.word = word
        self.frag = frag


word = [*pool]
#call the category.pool_elements funtion
pool_elements(key)
#fragment the chosen word from the category
frag = list(chosen_word)
my_guess(frag)
